# A nonstandard card

Free-form prose, no `- **Field**:` lines at all, archived without any
completion date — the scanner must fall back to the dir mtime (probe finding:
3 fabrikam archive cards look exactly like this).
